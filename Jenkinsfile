#!groovy
pipeline {
    agent {
        label "jenkins-agent-azure"
    }
    stages {
        stage('Build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
